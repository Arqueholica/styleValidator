services:
  - type: web
    name: docstyler
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8080
    disk:
      name: docstyler-data
      mountPath: /app/data
      sizeGB: 1
